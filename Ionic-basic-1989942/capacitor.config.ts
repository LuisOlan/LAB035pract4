import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Ionic-basic-1989942',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
